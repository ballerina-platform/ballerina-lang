package org.ballerinalang.test.mime;

import org.ballerinalang.mime.util.MultipartDataSource;
import org.ballerinalang.model.values.BStruct;
import org.testng.annotations.Test;

import java.util.ArrayList;

public class MultipartEncoderTest {

    @Test(description = "Write body parts...")
    public void testMultipartWriter() {
       /* ArrayList<BStruct> bodyParts = new ArrayList<>();
        bodyParts.add(MimeTestUtil.getJsonBodyPart());
        bodyParts.add(MimeTestUtil.getXmlFilePart());
        bodyParts.add(MimeTestUtil.getTextBodyPart());
        bodyParts.add(MimeTestUtil.getBinaryFilePart());*/


    }
}
