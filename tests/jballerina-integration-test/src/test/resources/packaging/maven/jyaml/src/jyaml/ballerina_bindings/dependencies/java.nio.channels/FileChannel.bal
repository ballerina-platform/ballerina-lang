// This is an empty Ballerina object autogenerated to represent the `java.nio.channels.FileChannel` Java class.
//
// If you need the implementation of this class generated, please use the following command.
//
// $ ballerina bindgen [(-cp|--classpath) <classpath>...] [(-o|--output) <output>] (<class-name>...)
//
// E.g. $ ballerina bindgen java.nio.channels.FileChannel



# Ballerina object mapping for Java abstract class `java/nio/channels/FileChannel`.
#
# + _FileChannel - The field that represents this Ballerina object, which is used for Java subtyping.
# + _SeekableByteChannel - The field that represents the superclass object `SeekableByteChannel`.
# + _ReadableByteChannel - The field that represents the superclass object `ReadableByteChannel`.
# + _ByteChannel - The field that represents the superclass object `ByteChannel`.
# + _GatheringByteChannel - The field that represents the superclass object `GatheringByteChannel`.
# + _InterruptibleChannel - The field that represents the superclass object `InterruptibleChannel`.
# + _Channel - The field that represents the superclass object `Channel`.
# + _Closeable - The field that represents the superclass object `Closeable`.
# + _AutoCloseable - The field that represents the superclass object `AutoCloseable`.
# + _WritableByteChannel - The field that represents the superclass object `WritableByteChannel`.
# + _AbstractInterruptibleChannel - The field that represents the superclass object `AbstractInterruptibleChannel`.
# + _Object - The field that represents the superclass object `Object`.
# + _ScatteringByteChannel - The field that represents the superclass object `ScatteringByteChannel`.
type FileChannel object {

    *JObject;

    FileChannelT _FileChannel = FileChannelT;
    SeekableByteChannelT _SeekableByteChannel = SeekableByteChannelT;
    ReadableByteChannelT _ReadableByteChannel = ReadableByteChannelT;
    ByteChannelT _ByteChannel = ByteChannelT;
    GatheringByteChannelT _GatheringByteChannel = GatheringByteChannelT;
    InterruptibleChannelT _InterruptibleChannel = InterruptibleChannelT;
    ChannelT _Channel = ChannelT;
    CloseableT _Closeable = CloseableT;
    AutoCloseableT _AutoCloseable = AutoCloseableT;
    WritableByteChannelT _WritableByteChannel = WritableByteChannelT;
    AbstractInterruptibleChannelT _AbstractInterruptibleChannel = AbstractInterruptibleChannelT;
    ObjectT _Object = ObjectT;
    ScatteringByteChannelT _ScatteringByteChannel = ScatteringByteChannelT;

    # The init function of the Ballerina object mapping `java/nio/channels/FileChannel` Java class.
    #
    # + obj - The `handle` value containing the Java reference of the object.
    function init(handle obj) {
        self.jObj = obj;
    }

    # The function to retrieve the string value of a Ballerina object mapping a Java class.
    #
    # + return - The `string` form of the object instance.
    function toString() returns string {
        return jObjToString(self.jObj);
    }
};


