
<a href="#{{name}}">
    <div class="param-sum">
        <div class="param-name {{#if isDeprecated}}strike{{/if}}">
            {{name}}
        </div>
        <div class="param-details">
            {{#if isDeprecated}}
                <span class="stab">Deprecated</span>
            {{/if}}
            {{{ description }}}
        </div>
    </div>
</a>