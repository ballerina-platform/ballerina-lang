<!DOCTYPE html>
<html>

{{> head }}

<body>
  <div class="ui grid">
    <div class="navi-wrapper two wide column"> 
      <div class="navi-wrapper-content">
          {{> logo }}
      </div>
    </div>
    <div class="fourteen wide column content-wrapper">
      <div class="main-wrapper">
        <h1>Modules</h1>
        <div class="main-wrapper">
          <table class="ui table">
             {{#if project.modules}}
            <tbody>
             {{#each project.modules}}
              <tr>
                <td class="module-title modules"><a href="{{id}}/index.html">{{id}}</a></td>
                <td class="module-desc">{{{ summary }}}</td>
              </tr>
              {{/each}}
            </tbody>
            {{/if}}
          </table>
        </div>
      </div>
    </div>
  </div>
</body>
