<!DOCTYPE html>
<html>

{{> head }}

<body>
<div class="ui grid">
    <div class="navi-wrapper two wide column">
        <div class="navi-wrapper-content">
            {{> logo }}
            <div class="primary-list">
                <h3>Annotations</h3>
                <ul>
                    {{#each module.annotations}}
                        <li>
                            <a href="#{{name}}">{{name}}</a>
                        </li>
                    {{/each}}
                </ul>
            </div>
            <div class="ui divider"></div>
            {{> currentModule }}
            {{> allModules }}
        </div>
    </div>

    <div class="fourteen wide column content-wrapper">
        <div class="main-wrapper">
            <h1>Annotations</h1>
            <div class="annotations">
                <div class="data-wrapper">
                    {{#each annotations}}
                        <div class="params-listing">
                            <ul>
                                <li><b>{{ name }}</b><span class="type">{{#if type}}{{{ typeName
                                        type }}}{{/if}}</span> {{ attachmentPoints }}</li>
                                <li>
                                    {{{ description }}}
                                </li>
                            </ul>
                        </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</div>
</body>
