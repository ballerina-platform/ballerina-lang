{{#partial "titleType" }}
    Constants
{{/partial}}

{{#partial "content" }}
    <div class="constants">
        <div class="data-wrapper">
            {{#each constants}}
                <div class="params-listing">
                    <ul>
                        <li id="{{ name }}">
                            <b {{#if isDeprecated}}class="strike"{{/if}}>{{ name }}</b>
                            <span class="type">  {{{ typeName type }}}</span> {{ value }}
                        </li>
                        <li>
                            {{#if isDeprecated}}
                                <span class="stab">Deprecated</span>
                            {{/if}}
                            {{{ description }}}
                        </li>
                    </ul>
                </div>
            {{/each}}
        </div>
    </div>
{{/partial}}

{{> layout}}
