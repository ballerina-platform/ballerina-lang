openapi: 3.0.1
info:
  title: serviceName
  version: 1.0.0
servers:
- url: /serviceName
paths:
  /user:
    get:
      summary: Get operation for the path /user
      operationId: operation1-resource1
      responses:
        200:
          description: Successful
          content:
            application/json:
              example: Ok
    put:
      summary: Put operation for the path /user
      operationId: operation1-resource2
      responses:
        200:
          description: Successful
          content:
            application/json:
              example: Ok
    post:
      summary: Post operation for the path /user
      operationId: operation2-resource1
      responses:
        200:
          description: Successful
          content:
            application/json:
              example: Ok
components: {}
