enum Color {
    RED, GREEN, BLUE
}

enum Vehicle {
    CAR,
    BIKE,
    VAN
}

enum Veg {
    POTATO,
    TOMATO,
    CARROT
}

enum Fruit {
    APPLE, MANGO
}

enum FuelType {
    PETROL,
    DIESEL
}
