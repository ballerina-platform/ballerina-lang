// Ballerina error type for {{exceptionName}}.

const {{exceptionConstName}} = "{{shortExceptionName}}";

type {{shortExceptionName}}Data record {
    string message;
    error cause?;
};

{{accessModifier}}type {{shortExceptionName}} error<{{exceptionConstName}}, {{shortExceptionName}}Data>;
