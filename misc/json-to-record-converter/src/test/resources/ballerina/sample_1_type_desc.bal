type NewRecord record {
    record { string question; (int|string|(int|string)[])[] options; string answer;}[] sports;
    record { string question; (decimal|int|string)[] options; stringa nswer;}[] maths;
};
