{{package}}
{{imports}}

public {{abstract}} class {{className}}{{type}} {{relationship}} {{parentClass}}{
{{#attributes}}private {{type}} {{name}};
{{/attributes}}

public {{className}}(STNode node, int position, NonTerminalNode parent) {
super(node, position, parent);
}

{{#attributeFunction}}public {{returnObject}} {{name}}() {
if ({{name}} != null) {
return {{name}};
}
{{name}} = {{parentMethod}}({{index}}){{#createFacadeCall}}.createFacade(getChildPosition({{index}}), this){{/createFacadeCall}};
return {{name}};
}
{{/attributeFunction}}

{{#switchFunction}}
public Node childInBucket(int bucket) {
switch (bucket) {
{{#caseList}}
case {{index}}:
return {{name}}();
{{/caseList}}
}
return null;
}
{{/switchFunction}}
}
