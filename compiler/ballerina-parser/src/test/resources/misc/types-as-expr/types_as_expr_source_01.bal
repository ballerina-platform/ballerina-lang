public function main() {
    boolean bol = a.ensureType(boolean);
    int intg = a.ensureType(int);
    byte byt = a.ensureType(byte);
    float flt = a.ensureType(float);
    decimal deci = a.ensureType(decimal);
    string str = a.ensureType(string);
    handle hdl = a.ensureType(handle);
    json jsn = a.ensureType(json);
    anydata anydataType = a.ensureType(anydata);
    any anyType = a.ensureType(any);
    never nev = a.ensureType(never);
}
