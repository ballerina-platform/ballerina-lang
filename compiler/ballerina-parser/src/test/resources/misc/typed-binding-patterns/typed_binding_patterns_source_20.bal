T error(a, b) = x;
T error T(a, b) = x;
var error(a, b) = x;
int error T(a, b) = x;

function foo() {
    T error(a, b) = x;
    T error T(a, b) = x;
    var error(a, b) = x;
    int error T(a, b) = x;
    int [error(i = i)] = y;
}
