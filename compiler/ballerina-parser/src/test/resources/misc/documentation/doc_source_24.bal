// Intentionally kept no whitespaces between hash and triple backticks
# Prints value(s) to the STDOUT.
#```ballerina
# io:print("Start processing the CSV file from ", srcFileName);
#```
public function print(){
}
