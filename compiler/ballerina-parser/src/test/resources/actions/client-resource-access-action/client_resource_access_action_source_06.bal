function foo() {
    var b = a->/7;
    
    var b = a->/games->;
    var b = a->/666[pathSegment];
    b = a->/games[game]/details;
    _ = a->/[...restSegment;
    b += a->games/[...restSegment];

    var c = a->/.get j;
    _ = a->/&&();
    b = a->(arg1, arg2);
    c = a->.post(arg1, arg2);
    
    match a->/gamespath2 {
        _ => {
        }
    }

    match a->/[pathSegment {
        _ => {
        }
    }

    match a->/.post(arg1, arg2 {
        _ => {
        }
    }

    foreach var item in a->55/games {
    }

    foreach var item in a->/tgr[...restSegment {
    }

    foreach var item in a->/7676(arg1, arg2) {
    }
}
