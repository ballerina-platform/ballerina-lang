function foo() {
    worker E {
        foreach var err in <- A | B {
        }
    }

    <- function | function;
    <- A | B | C | D;
    <- A | function | function | D;
    a = <- function | A;
    a = <- A | B | C;
    int b = <- B | C;
}
