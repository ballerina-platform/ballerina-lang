function foo() {
    worker E {
        foreach var err in <- stream (A | B) {
        }
    }

    <- stream (function | function);
    <- stream (A | B | C | D);
    <- stream (A | function | D);
    a = <- stream (function | A);
    a = <- stream (A | B | C);
    int b = <- stream (B | C);
}
