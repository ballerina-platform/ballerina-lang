transactional invalidtok function() a;

function foo(transactional function(8) f) returns transactional isolated {
    transactional 7 % isolated  function(a,b) c;
    transactional function(a b) c;
    transactional (a,b) c;
    ggrrg transactional isolated function()|MyType[] d;
    [transactional function, int, restType...] e;

    boolean b = x is transactional returns int;
    f2 = <transactional isolated function int, int a) returns int> f1;
}

type bar record {
    transactional 45544ghdhgh &! function() returns int func;
};
