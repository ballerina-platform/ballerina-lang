function foo() {
    x = re `[^\^]`;
    x = re `[^\$]`;
    x = re `[^\\]`;
    x = re `[^\.]`;
    x = re `[\*]`;
    x = re `[^\+]`;
    x = re `[\?]`;
    x = re `[\(]`;
    x = re `[^\)]`;
    x = re `[\[]`;
    x = re `[^\]]`;
    x = re `[\{]`;
    x = re `[^\}]`;
    x = re `[^\|]`;
    x = re `\\-`;
}
