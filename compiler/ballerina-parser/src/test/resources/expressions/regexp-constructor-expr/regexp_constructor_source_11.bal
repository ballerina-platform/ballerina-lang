function foo() {
    x = re `[^\d]`;
    x = re `[^\D]`;
    x = re `[^\s]`;
    x = re `[^\S]`;
    x = re `[^\w]`;
    x = re `[^\W]`;
    x = re `[\d]`;
    x = re `[\D]`;
    x = re `[\s]`;
    x = re `[\S]`;
    x = re `[\w]`;
    x = re `[\W]`;
}
