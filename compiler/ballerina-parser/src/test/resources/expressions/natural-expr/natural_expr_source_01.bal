public function main() {
    int a = natural model(m) {
        this is a test prompt and also support interpolation ${"hello"}
    };
    int a = natural {
        this is a test prompt
        in multiple lines
        close braces can be escaped like this \} if u want it inside a prompt
        when using backslashes, you need to escape them like this \\. You can write any code inside the prompt
              function getStringValue(int val) returns string {
              \}
    };
}
