function testIterableOperation() {
    string outputNameString = from var person in personList
        let var test = 12,
         
}

type Person record {|
    string firstName;
    string lastName;
    int age;
|};
