const annotation name on object

Animal a = new Animal();

const annotation name on s //<cursor>

Animal a = new Animal();

const annotation name on source o //<cursor>

Animal a = new Animal();

const annotation name on source object f //<cursor>
// Intentionally left with EOF as next token
