resource function processUser rf() returns string {
    return string `${user} is ${user} `;
}

object {
    resource function processUser rf() returns string {
        return string `${user} is ${user} `;
    }
}
