#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")
package ${PACKAGE_NAME};
#end

import ballerina/io;

documentation {
   Prints `Hello World`.
}
function main(string... args) {
    io:println("Hello World!");
}