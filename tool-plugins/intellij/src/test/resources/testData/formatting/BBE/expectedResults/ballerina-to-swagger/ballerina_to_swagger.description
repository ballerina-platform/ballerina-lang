// The Ballerina to Swagger tool is used to export an Open API Specification(OAS) v3 definition of a Ballerina service.
// This tool accepts the following command line arguments,
// `-s | --service` If the provided file contains multiple services, define a specific service that needs to be exported.
// `-o | --output` The destination path to write the exported OAS definition.
