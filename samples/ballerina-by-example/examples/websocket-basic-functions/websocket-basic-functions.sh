# To run the program, put the code in `websocket-basic-functions.bal`
# and use `$BALLERINA_HOME/bin/ballerina run service websocket-basic-functions.bal` command.

$ $BALLERINA_HOME/bin/ballerina run service websocket-basic-functions.bal

# Now that we can run this program using any WebSocket client with "ws://host:port/functions/ws"
