// Ballerina SQL Connector is used to access and manipulate data in the relational
// databases. It supports update, select, batchUpdate, call, updateWithGeneratedKeys
// and close actions. This sample use MySQL DB and before running the sample copy the
// MySQL JDBC driver to the BALLERINA_HOME/bre/lib folder and change the DB connection
// properties in the ballerina sample.