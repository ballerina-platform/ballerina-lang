# To run the program, put the code in `hello-world-server.bal`
# and use `$BALLERINA_HOME/bin/ballerina`.

$ $BALLERINA_HOME/bin/ballerina websocket-basic-endpoint.bal

# Now that we can run basic Ballerina programs, let's
# learn more about the language.

