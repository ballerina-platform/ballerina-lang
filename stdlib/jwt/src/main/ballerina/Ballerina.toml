[project]
org-name = "ballerina"
version = "0.0.0"

[platform]
target = "java"

    [[platform.libraries]]
    artifactId = "jwt"
    version = "@project.version@"
    path = "./lib/ballerina-jwt-@project.version@.jar"
    groupId = "ballerina"
    modules = ["jwt"]
