CREATE TABLE Customers(
  customerId INT GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1),
  firstName  VARCHAR(300),
  lastName  VARCHAR(300),
  registrationId INT,
  creditLimit DECIMAL(10,2),
  country  VARCHAR(300),
  PRIMARY KEY (customerId)
);
/
