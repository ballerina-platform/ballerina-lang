{{#imports }}
    {{{.}}}
{{/imports}}

{{#moduleDclns }}
    {{{.}}}
{{/moduleDclns}}

function println(any|error... values) { }
function recall_h(string name) returns any|error { }
function memorize_h(string name, any|error value) { }
function sprintf(string template, any|error... values) returns string { return ""; }

{{#varDclns}}
    {{#isAny }}
        {{{prefix}}} {{{type}}} {{{name}}} = // value
        <{{{type}}}> checkpanic recall_h("x");
    {{/isAny}}
    {{^isAny }}
        {{{prefix}}} {{{type}}} {{{name}}} = // value
        <{{{type}}}> recall_h("x");
    {{/isAny}}
{{/varDclns}}
any|error {{{exprVarName}}} = ();

public function main() {
    // Redefine to restrict user
    any|error {{{exprVarName}}} = ();

    // We have to define the variable in a function because
    // some expressions are not allowed in module elevel as initializers
    {{{lastVarDcln}}}

    // Use the java module to suppress import not used error
    _ = java:JavaClassNotFoundError;
}
