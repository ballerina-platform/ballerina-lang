package ballerina.net;

// do we need this?? Not sure there's a need for a unified endpoint types
// http connectors (aka stubs for web apis) can import httpendpoint if they
// want to expose http get/put/etc. too.
// other endpoints have no commonality that I can see - so why do we need a
// a common endpoint type?
