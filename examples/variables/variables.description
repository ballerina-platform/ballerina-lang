// Ballerina allows two ways to declare variables.
// You can either use the `var` keyword followed by the variable name, or use the initializer expression and
// type name followed by the variables where you can optionally use the initializer expression.
