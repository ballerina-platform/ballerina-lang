// Ballerina programs are written in files whose extension is “.bal” and the files names have no semantics.
// These source files can be grouped into packages. The code within a package can refer to any package-level symbol (name) declare
// in the same package.
