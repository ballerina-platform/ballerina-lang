// Ballerina locks are used for concurrency management. Encapsulating blocks of statements will acquire
// the locks for each global or service level variable reference used within those blocks of statements.
