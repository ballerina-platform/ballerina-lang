// Ballerina SQL Connector is used to access and manipulate data in the relational
// databases. This sample use MySQL DB and before running the sample copy the
// MySQL JDBC driver to the BALLERINA_HOME/bre/lib folder and change the DB connection
// properties as required.