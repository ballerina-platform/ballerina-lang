# At the command line, use the `broker.sh` or `broker.bat` command to start the Ballerina message broker.
$ broker.sh

# At the command line, navigate to the directory that contains the
# `.bal` file and run the `ballerina run` command.
$ ballerina run mb_simple_topic_message_publisher.bal
