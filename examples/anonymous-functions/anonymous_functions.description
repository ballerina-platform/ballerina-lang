// In Ballerina, anonymous functions are a syntactic shortcut for defining inline functions.
// The only difference between an anonymous function and a normal function definition is that 
// the only missing part is the function name.
