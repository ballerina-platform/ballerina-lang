// A record is a set of named fields where each field can be of a different type. When the set of fields is fixed,
// the record is called a "sealed record" and when it is unfixed, it is called an "open record".
