// The type 'table' is used to hold tabular data such as the result of
// an SQL database query and provide cursor-based access to the data. This sample uses
// MySQL. Before running the sample, copy the MySQL JDBC driver to the
// BALLERINA_HOME/bre/lib folder and change the DB connection properties as required.
