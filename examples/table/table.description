// The type 'table' is a data structure which organizes information into rows and columns. This example shows how to
// create an in-memory table using a type constraint, insert data to it and access/delete the data.
