// Start the introspection server on 9095 with the resource path
// `/oauth2/token/introspect` and invoke the service using "cURL".
// Note that it is required to provide the correct bearer authentication header
// with the cURL command. It will get validated with introspection server.
$ curl -vk -H "Authorization: Bearer YWRtaW46MTIz" \
https://localhost:9090/hello/sayHello
Hello, World!!!
