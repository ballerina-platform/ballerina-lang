# To run this sample, navigate to the directory that contains the
# `.bal` file and execute the `ballerina run` command.

# Use the ballerina `run` command to invoke the `main` function, specifying `add`
# as the string argument for `operation` and `10` and `20` as the integer arguments
# with which the operation is to be performed.
$ ballerina run the_main_function.bal add 10 20
Result: 30

# Use the ballerina `run` command to invoke the `main` function, specifying `subtract`
# as the string argument for `operation`, `40` as the integer argument for the defaultable
# parameter `initialValue`, and `10` and `20` as the integer arguments with which the
# operation is to be performed.
$ ballerina run the_main_function.bal subtract -initialValue=40 10 20
Result: 10

# Use the ballerina `run` command to invoke the `main` function, specifying `unknown op`
# as the string argument for `operation`.
$ ballerina run the_main_function.bal "unknown op"
Error: Unknown Operation
