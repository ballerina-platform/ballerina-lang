//The `stream` type represents a stream of events, which allows publishing events to the stream and subscribing to receive
// events that are published to the stream.
//Streams can be declared as constrained by a type, or as unconstrained streams that accept events of any type.
