//The `stream` type represents a stream of events that allow you to publish events to the stream, and subscribe to receive
// events that are published to the stream.
//Streams declarations can be constrained by a type, or declared as unconstrained streams that accept events of any type.
