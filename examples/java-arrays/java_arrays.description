// Java arrays are represented by the `handle` type in Ballerina. They can be manipulated using the
// `getArrayElement`, `setArrayElement` and `getArrayLength` methods provided by the `ballerina/java` module.
