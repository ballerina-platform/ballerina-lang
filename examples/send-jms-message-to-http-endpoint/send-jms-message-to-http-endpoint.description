// This example shows how to send a JMS message, consumed by the JMS server connector,
// to an HTTP endpoint. A JSON payload from the JMS broker is expected and that payload is then sent
// back to an HTTP endpoint.
