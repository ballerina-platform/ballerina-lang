// Ballerina XA transactions are used when the transaction is happening over
// two or more databases or JMS endpoints. This sample uses two H2 DBs (these
// need to be created prior to running this sample). Before running the sample, 
// change the DB connection properties as required.
