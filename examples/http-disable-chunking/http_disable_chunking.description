//This sample demonstrates how to change the chunking behavior of a Ballerina HTTP client endpoint. By default, the HTTP client sends content-length messages.
//If the message size is larger than the buffer size (8K), messages are chunked. Chunking can be disabled using the connector options.
