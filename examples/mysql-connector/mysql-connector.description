// The Ballerina MYSQL Connector is used to access and manipulate data in MYSQL
// databases. Before running the sample, copy the
// MySQL JDBC driver to the `BALLERINA_HOME/bre/lib` folder and change the DB connection
// properties as required.
