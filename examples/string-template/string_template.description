// String templates are string literals, which allow embedded expressions.
// The placeholders for expressions are indicated by the dollar symbol followed by an open and close curly braces `${}`.
// You can enclose a string literal with the keyword `string` followed by two back-tick characters.
