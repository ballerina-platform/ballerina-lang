// Ballerina functions can have defaultable parameters. Defaultable parameters are optional parameters that have a default 
// value. A function with defaultable parameters can be invoked without providing values for the defaulable parameters.
// When invoking the function, the defalutable parameters have to be passed as key-value pairs.
// The placement of defalutable parameters within a function call does not matter.
