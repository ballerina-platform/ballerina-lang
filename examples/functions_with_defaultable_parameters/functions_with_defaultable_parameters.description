// Ballerina functions can have defaultable parameters. They are optional parameters with a default 
// value. Such a function can be invoked without providing a value for those defaulable parameters.
// When invoking the function, The defalutable parameter needs to be paassed as key-value pairs, and 
// their position within the function call does not matter.
