// The check is a unary expression that is used to handle errors.
// The `check` expression checks the result of the sub expression and return if the result is `error`.
// Hence, check can only be used in functions which has `error` in it's return type.
