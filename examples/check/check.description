//The check is a unary expression, that is used to handle errors. Check expression removes error type from the sub expression's result and handle error case separately. Hence check can be considered as a built-in match.
