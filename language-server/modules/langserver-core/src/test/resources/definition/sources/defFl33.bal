function testConditionalStatement() {
    
    boolean ifCondition = true;
    boolean elseIfCondition = true;
    
    if (ifCondition) {
        printHello();
    } else if (elseIfCondition) {
        printHello();
    } else {
        printHello();
    }
}

function printHello() {
    // Logic Goes Here
}
