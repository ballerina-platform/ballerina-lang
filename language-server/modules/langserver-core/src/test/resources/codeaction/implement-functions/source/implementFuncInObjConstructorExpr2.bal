type Student object {
    function getName() returns string;

    function getId() returns int;
};

function test() {
    if true {
        Student Impl = object Student {
            
        };
    }
}
