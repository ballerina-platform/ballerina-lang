function name () {
    int value = 0;
    if(   (value > 10  ) ){

    } else if(   value<=0 ){

    } else if( ( value==0 )&&( value<0 ) ) {

    } else if ( ( value==0 )&&( value<0&&( value>1) ) ) {

    }
}