NAME
       ballerina-clean - Remove the artifacts created during the build

SYNOPSIS
       bal clean


DESCRIPTION
       Remove the artifacts generated inside the `target` directory during
       the build. This command is only valid from within a Ballerina package.
