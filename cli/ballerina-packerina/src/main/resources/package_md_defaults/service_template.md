Serves "Hello Ballerina!" over HTTP GET.
[//]: # (above is the package summary)

# Package Overview
Serves "Hello Ballerina!" as a response from an HTTP service when a GET is invoked to `/hello/sayHello` path.
