NAME
    Ballerina version - Print the Ballerina version

SYNOPSIS
    ballerina <-v|-version|version>

DESCRIPTION
     The `ballerina version` command prints the Ballerina version, which is currently in use.

     Note that `ballerina -v` and `ballerina –version` are  
     identical to `ballerina version`. 

OPTIONS
     -v
     -version
         Shorthand syntax for `ballerina version`. 

EXAMPLES
     Print the current Ballerina version installed in your machine:
     $ ballerina -v
