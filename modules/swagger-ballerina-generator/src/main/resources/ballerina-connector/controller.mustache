package {{apiPackage}};

import ballerina.lang.messages;
import ballerina.lang.string;
import ballerina.lang.system;
import ballerina.net.http;
import ballerina.net.uri;
import ballerina.util;

connector {{classname}} () {

http:ClientConnector baseEP = new http:ClientConnector("{{basePath}}");

{{#operations}}
{{#operation}}
action {{nickname}} ({{classname}} t, string msg) (message) {
        message request = {};
        message response;
        messages:setHeader(request, "User-Agent", "Ballerina-1.0");
        messages:setStringPayload(request, msg);
        response = http:HTTPConnector.{{httpMethod}}(baseEP, {{path}} , request);
        return response;		
}

{{/operation}}
{{/operations}}
}
