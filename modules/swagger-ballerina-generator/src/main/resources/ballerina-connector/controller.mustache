import ballerina.lang.messages;
import ballerina.lang.system;
import ballerina.net.http;
import ballerina.lang.jsonutils;
import ballerina.lang.exceptions;
import ballerina.lang.arrays;

connector {{classname}} (string text) {

http:ClientConnector endpoint = create http:ClientConnector("{{basePath}}");
{{#operations}}
{{#operation}}
    action {{nickname}} ( message msg )
    (message) {
        //Create sample json message as follows.
        //message request = {};
        //messages:setHeader(request, "Content-Type", "application/json");
        //Users can process input message msg as per requirements.
        message response;
        response = http:ClientConnector.{{httpMethod}}(endpoint, http:getRequestURL(message) , msg);
        return response;
    }
{{/operation}}
{{/operations}}
}