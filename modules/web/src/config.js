export default {
    app: {
        container: '.app-container',
        plugins: [],
    },
    // overridden plugin specific configs - if any.
    // plugin-id will be the key
    pluginConfigs: {},
};
